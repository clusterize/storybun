export default { title: "Fixtures/Empty" };

// A story that renders nothing at all, the shape the capture must refuse
// without taking the rest of the run down with it.
export const Nothing = () => null;
